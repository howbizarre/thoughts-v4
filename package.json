{
	"name": "thoughts-v4",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev --port 7410",
		"generate": "nuxt generate",
		"preview": "npm run build && wrangler dev",
		"postinstall": "nuxt prepare",
		"deploy": "npm run build && wrangler deploy",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"@nuxt/ui": "^3.3.0",
		"@nuxtjs/i18n": "^10.0.3",
		"nuxt": "^4.0.2",
		"vue": "^3.5.18",
		"vue-router": "^4.5.1"
	},
	"devDependencies": {
		"nitro-cloudflare-dev": "^0.2.2",
		"nitropack": "^2.12.4",
		"wrangler": "^4.26.1"
	}
}
